import React from "react";
import "./styles.css";

const Header: React.FC = () => {
  return (
    <div className="header">
      <span className="header-title">Glasses Dashboard</span>
    </div>
  );
};

export default Header;
